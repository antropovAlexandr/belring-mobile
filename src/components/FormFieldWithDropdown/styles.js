import { StyleSheet } from 'react-native'
import {black02, black087, black06, grayMedium, whiteGray} from '../../consts/colors'

export default StyleSheet.create({
    dropdown: {
        marginBottom: 10,
    },
});

